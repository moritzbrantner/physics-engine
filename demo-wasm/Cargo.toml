[package]
name = "physics-engine-demo"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[features]
default = []
exact-reference = ["physics-engine/exact-reference"]

[dependencies]
physics-engine = { path = ".." }
